<!DOCTYPE html>
<html class="m-0 p-2">
	<head>
		<title><%= content_for(:title) || "Prueba Apps Rails" %></title>
		<meta name="viewport" content="width=device-width,initial-scale=1">
		<meta name="apple-mobile-web-app-capable" content="yes">
		<%= csrf_meta_tags %>
		<%= csp_meta_tag %>

		<%= yield :head %>

		<link rel="manifest" href="/manifest.json">
		<link rel="icon" href="/icon.png" type="image/png">
		<link rel="icon" href="/icon.svg" type="image/svg+xml">
		<link rel="apple-touch-icon" href="/icon.png">
		<!-- Bootstrap 5.3.3 -->
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
			rel="stylesheet"
			integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
			crossorigin="anonymous">
		<%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
		<%= javascript_importmap_tags %>
	</head>

	<body class="bg-info bg-opacity-25">
		<head>
			<nav class="navbar navbar-expand-lg bg-black bg-opacity-25 rounded">
				<div class="container-fluid">
					<%= link_to "Home", "/tweets", class:"navbar-brand" %>
					<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
						<span class="navbar-toggler-icon"></span>
					</button>
					<div class="collapse navbar-collapse" id="navbarSupportedContent">
						<ul class="navbar-nav me-auto mb-2 mb-lg-0">
							<li class="nav-item">
								<a class="nav-link active" aria-current="page" href="#"></a>
							</li>

							<li class="nav-item">
								<%= link_to "Nuevo tweet", new_tweet_path, class: 'nav-link' %>
							</li>
						</ul>
						<!-- Formulario de búsqueda -->
						<%= form_with url: search_tweets_path, method: :get do %>
						<div class="d-flex" role="search">
							<%= text_field_tag :query, params[:query], placeholder: "Buscar por usuario o tweet", class: 'form-control me-2' %>
							<%= submit_tag "Buscar Tweet", class:'btn btn-outline-success' %>
						</div>
						<% end %>
					</div>
				</div>
			</nav>
		</head>
		<%= yield %>
	</body>
	<!-- Bootstrap 5.3.3 -->
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
		integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
		crossorigin="anonymous">
	</script>
</html>
